# eZ Launchpad with docker compose compatibility

eZ Launchpad is a CLI tool to start an eZ Platform project in 5 min on top of a full Docker stack.

## this is a fork created to achieve the docker compose compatibility, as ezlaunchpad worked with docker-compose command instead docker compose




